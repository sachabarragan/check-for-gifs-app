import React from 'react';
import ReactDOM from 'react-dom/client';
import { CheckForGifApp } from './CheckForGifApp';

import './styles.css';


ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CheckForGifApp />
  </React.StrictMode>
);
